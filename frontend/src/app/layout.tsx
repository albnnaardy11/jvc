import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "JVC Quranic SuperApp — Premium AI-Human Memorization & Tilawah Ecosystem",
  description: "Enterprise-grade hybrid AI-human ecosystem for Quranic memorization (Hifz) and recitation (Tilawah) built for global scale. Features smart trackers, Google Vertex AI (Chirp) feedback, Mosque Explorer, Sparing Tilawah, and talent marketplaces.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
